.content
  .left-content
    .left-header
      .name 
        aono minoru 
      .menu-icons
        = link_to new_group_path do
          = fa_icon 'pencil-square-o', class: 'icon'
        = link_to edit_user_path(current_user) do
          = fa_icon 'cog', class: 'icon'
    .groups
      .group
        - current_user.groups.each do |group|
          = link_to group_rooms_path(group) do
            .group__name
              = group.name
            .group__message
              メッセージはまだありません。
  .chat-main
    .main-header
      .main-header_left-box 
        Hello world!
        .main-header_left-box_member_list 
          Member
      -# = link_to edit_group_path(@group) do
      -#   = edit type: 'submit', value: 'Edit'

    .content-main__chat
      .data-chat_id
        .chat_upper-info
          %p.chat_upper-info_talker hello
          %p.chat_upper-info_date 2019
        %p.message_text hey!
    .footer   
      .input-box
      %input.login-input-text{name: "type-a-message", placeholder: "type a message", type: "text", value: ""}/
      .menu-icons
        = link_to "#", class: "content__btn" do
          = fa_icon 'picture-o'
      %input.submit{type: "submit", value: "send"}